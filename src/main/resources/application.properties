# Core Properties
info.app.name=My Super Cool App
info.app.description= Cool Application
info.app.version=1.0.0

first.name= Satheesh
last.name= Venkatachalam

# Web Properties
server.port=8080
server.servlet.context-path=/myApp
server.servlet.session.timeout = 30m


# Actuator Properties
# use wildcard * to expose all endpoints or with comma-delimited list
management.endpoints.web.exposure.include=*
management.info.env.enabled=true

# Exclude individual endpoints with comma-delimited list
management.endpoints.web.exposure.exclude=health


# Security Properties
spring.security.user.name = admin
spring.security.user.password = admin123


spring.main.lazy-initialization=false

# Data Properties, Database